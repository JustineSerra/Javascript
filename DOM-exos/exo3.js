/*

const elements = document.querySelectorAll(`.info`);
//on sélectionne tous les éléments qui ont la classe "info"
for (let i=0; i<elements.length; i++) {
//la boucle for va parcourir tous les élements de 0 jusqu'à la fin de sa longueur. 
    elements[i].textContent="Modifié";
//elle va ensuite remplacer le texte de chaque élément par "Modifié"
}

*/