/*

const elementLi = document.createElement("li");
elementLi.textContent = "Elément ajouté";
//définition du textContent de elementLi donnée par l'exo.

const liste = document.getElementById("liste");
liste.appendChild(elementLi);
//appendChild permet d'ajouter un élément à un parent (ici le parent c'est liste).

*/